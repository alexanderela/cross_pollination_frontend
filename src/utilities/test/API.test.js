import * as API from '../API';

describe('API', () => {
	describe(('fetchData', () => {
		beforeEach(() => {
			it('should call fetch with the given URL', () => {})
			it('Should return json\'d response from fetch', () => {})
		})
	}))

	describe(('fetchCountries', () => {
		beforeEach(() => {
			it('should call fetchCountries with the correct URL', () => {})
			it('should return a resolved array', () => {})
		})
	}))

	describe(('loginUser', () => {
		beforeEach(() => {
			it('should call fetch on path /api/users', () => {})
			it('should pass an options object with stringified user data to fetch', () => {})
			it('Should return json\'d response from response', () => {})
		})
	}))

	describe(('createUser', () => {
		beforeEach(() => {
			it('should call fetch with the correct arguments', () => {})	
		})
	}))
})
